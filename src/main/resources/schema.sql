DROP TABLE IF EXISTS coffee;

CREATE TABLE coffee (
                        id int PRIMARY KEY,
                        blend VARCHAR(20),
                        strength VARCHAR(20),
                        origin VARCHAR(20)
);
